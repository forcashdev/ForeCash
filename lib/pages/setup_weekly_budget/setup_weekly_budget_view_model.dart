import 'package:flutter/material.dart';
import 'package:for_cash/pages/select_calendar_page/mobile_select_calender_page.dart';

class SetupWeeklyBudgetViewModel {
  dynamic state;

  SetupWeeklyBudgetViewModel(this.state) {
    this.state = state;
  }

  void onTapOfNext() {
    // Navigator.of(state.context).push(
    //     MaterialPageRoute(builder: (builder) => MobileSelectCalenderPage()));
  }
}
